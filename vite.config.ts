import { sveltekit } from '@sveltejs/kit/vite';
import { defineConfig } from 'vite';

export default defineConfig({
	plugins: [sveltekit()],
	base: '/Beijing-corn87.github.io/' // Replace with your repository name, e.g., '/my-portfolio/'
});
